// PCF Dev Proxy — Hot Reload extension
// Strips CSP headers from *.dynamics.com so the HMR client can connect to the local proxy.
console.log("[PCF-HMR] Extension loaded — CSP headers will be stripped from *.dynamics.com");
